# Hướng dẫn duyệt dev-portal sau V2-01/02/03 — cái gì đã đổi, cái gì chưa

Build đang phục vụ: `feat/execution_loop@6a323a4`, cờ preview bật. Số liệu từ `git diff` ba commit
V2 (`276dd5a`, `15b7bdb`, `2168db7`, `6a323a4`): **11 file màn hình bị chạm, tổng 113 dòng** — toàn bộ
là nối handler/điều hướng, **không có dòng nào đổi bố cục**. Bố cục 17 màn vẫn là composition ngày
2026-08-24 mà anh đã từ chối; V2-04 (Paper) mới bắt đầu dựng lại, và V2-05…08 mới lan ra các màn khác.

## Nên đánh giá — ba lớp đã thay, áp lên MỌI màn

| Lớp | Đổi gì | Nhìn ở đâu |
|---|---|---|
| **Workspace (V2-01)** | một hệ Carbon liền cho topbar · sidebar · canvas · rail từ Approval Inbox tới Live; selector đọc mode hiệu lực và disabled trên route Execution; preference Research/Planning không bị ghi đè; sidebar `PREVIEW` thay `SOON`; banner một dòng tiếng Anh + Details; breadcrumb có entity | **bất kỳ** route Execution, bật preference *Research Light* rồi vào — không còn seam; rời ra `/` thì shell sáng lại |
| **Chữ (V2-02)** | thang 12 vai §5.2 khoá cứng; hết mono 10px toàn màn; title 24 sans, KPI 24 mono, section 15 sans, body 13, meta/id 11 mono; uppercase chỉ ở table header | **mọi** màn — đọc thử một panel bất kỳ: tiêu đề còn là mono uppercase 10px không? |
| **Tương tác (V2-03)** | mọi control enabled có hậu quả; state tab/filter/scope vào URL; điều hướng canonical; mô phỏng công khai "Simulated · …"; screenId vào Inspector | 5 màn ở bảng dưới + CC/Queue/Incident/R1/R2 |

## Từng màn — cái gì đổi thật, cái gì chưa

| Màn | Route | Đổi trong V2-01..03 | **Chưa đổi** (đừng chấm) |
|---|---|---|---|
| Paper Workbench | `/deployments/paper/dep_94` | tab → URL; *Request exit* điều hướng có `?from=`; load older mô phỏng; Admin actions → drawer | bố cục chồng 12 khối, chart **vẫn khung trống**, lineage vẫn trên scan path — V2-04 |
| Alpha 360 | `/deployments/alphas/av_2041` | scope venue lọc bảng + KPI "not published for scope"; row → workbench đúng stage; account → Account 360; tab → URL | 12 tile **vẫn khung trống**; bố cục — V2-08 |
| Portfolio 360 | `/deployments/portfolios/PF-CRYPTO` | tab → URL; lens; holdings alpha/account → điều hướng | chart ρ/influence map/DD overlap **chưa có** — V2-08 |
| Account/Broker 360 | `/deployments/accounts/acct-live-grid-v21` | Sync now / Dry-run mô phỏng công khai (persona operator) | bố cục — V2-08 |
| Full Blotter | `/deployments/blotter` | filter → URL; reset cross-filter; expand row funnel; load older (chỉ trên All — fixture chỉ có cursor ở đó) | Columns/Export, footer tổng theo tiền tệ — V2-08 |
| Command Center | `/execution` | hàng ranked click → màn sở hữu (href server); hàng không href → disabled + lý do; head dùng vai title (pilot V2-02) | bố cục triage-first — V2-07 |
| Operations Queue | `/execution/operations` | hàng → chọn (rail theo hàng); hop Queue→Incident hiện **disabled + BR-EX-33** vì contract thiếu | bố cục, rail cảnh báo — V2-07 |
| Incident Detail | `/execution/operations/incidents/inc_44` | op row → Action Drawer mang `?operation=` | tabs evidence/timeline — V2-07 |
| Gate R1 / R2 | `/governance/approvals/AP-201/r1`, `/AP-352/r2` | *Approve with condition* disabled kèm lý do tới khi đính điều kiện | evidence chart **vẫn khung trống**, prose — V2-05 |
| Approval Inbox · Paper Exit · Sandbox · Canary · Live · VNM · Admin Drawer | — | **chỉ** ba lớp toàn cục ở trên (Carbon shell, chữ, banner) | toàn bộ bố cục — V2-05/06/07 |

## Câu hỏi đúng để chấm ở bước này

1. Đứng ở route Execution với preference Research: **còn seam nào không?** (V2-01 gate)
2. Chữ: **có chỗ nào còn đọc như terminal dump không** — tiêu đề, nút, nhãn? (V2-02 gate)
3. Bấm bất kỳ control enabled nào trên 5 màn bảng trên: **có gì xảy ra không** — đổi URL, đổi nội dung, hoặc dòng "Simulated · …"? (V2-03 gate)
4. Fixture group `V2 anatomy — the shared page skeleton on Paper data` ở `/execution/_fixtures`: **hình dạng trang mà V2-04 sẽ dựng cho Paper** — masthead/KPI/tabs/rail/provenance/terminal. Đây là thứ đáng góp ý sớm nhất, vì Paper sẽ theo đúng khung này.

Câu hỏi **chưa đúng lúc**: "màn X bố cục rối / chart trống / nhiều chữ" — đúng, và chưa được sửa; đó là
V2-04 trở đi, với gate duyệt hình ảnh của anh sau lát cắt Paper.
