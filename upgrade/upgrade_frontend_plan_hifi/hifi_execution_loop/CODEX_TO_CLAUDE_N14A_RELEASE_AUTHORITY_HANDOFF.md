# Codex → Claude: N14A source-dark release authority

Backend status: `N14A_COMPLETE_SOURCE_DARK / PRODUCTION_INACTIVE`  
Frontend authority change: none  
Execution source/profile change: none

## What Claude may rely on

- Dev remains `dev-portal.primusspark.com`, project `portal`, port 8080 and its
  own state. Stable remains `portal.primusspark.com`, project `portal-stable`,
  port 18081 and separate volumes.
- Stable bytes are selected only by an accepted six-service digest manifest.
- An N14A release decision never makes an Execution screen source-backed. All
  Projection, Query, SSE and command flags remain false until their B lanes.
- Fixture/unavailable/partial/denied labels and disabled command behavior stay
  exactly as before.

## Claude's parallel work

Continue the Execution Loop V2 composition/refactor against dev and typed
fixtures. Do not wait for N14B. In particular:

- do not expose release hashes in normal workbench mastheads, KPI strips or
  context rails; provenance belongs in a compact maintenance/diagnostics view;
- do not replace unavailable source facts with demo financial values;
- do not enable an action because a Portal release was accepted;
- keep one Carbon Execution surface, consistent typography/density and honest
  per-panel source/freshness states;
- test dev routes only and never point a fixture build at stable state.

No frontend API contract was added in N14A. If Claude later needs a compact
release diagnostics screen, record it through the unified BR-EX intake before
adding fields or presenting hashes.

## B-lane handoff later

N14B will provide an exact target-profile compatibility result, not a generic
"release green" boolean. Claude should consume only the resulting typed
profile/capability states after N13B/N14B acceptance; until then every
source-backed screen remains visibly inactive.

