# Partial fixture

Use fixture `partial_source_page` in the frozen E4/E5 fixture registries.
`PARTIAL` must remain partial in the Portal BFF and cannot be rendered as a
complete zero or replay range.
