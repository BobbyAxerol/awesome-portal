# Empty fixture

Use fixture `empty_complete` in `e4-golden-fixtures.v1.json` and
`e5-golden-fixtures.v1.json`. An empty result is valid only with the source
envelope `AVAILABLE` plus `COMPLETE`; it never proves empty history.
