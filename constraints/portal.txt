# Stack-wide runtime constraints. Keep this aligned with the child application's
# declared dependency and use it in CI/image builds so every environment runs
# the same QuantBT engine release.
quantbt-engine==1.0.8
